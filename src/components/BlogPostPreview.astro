---
interface Props {
  title: string;
  description: string;
  date: string;
  url: string;
  image?: string;
  base: string;
}

const { title, description, date, url, image, base } = Astro.props;

// Remove base from image path as it's causing issues
const formattedImage = image;
const formattedDate = new Date(date).toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'long',
  day: 'numeric'
});
---

<article class="post-preview">
  <a href={url} class="post-link">
    <h2>{title}</h2>
    <time datetime={date}>{formattedDate}</time>
    {formattedImage && (
      <div class="image-container">
        <img src={formattedImage} alt={`Preview for ${title}`} loading="lazy" decoding="async" />
      </div>
    )}
    <p>{description}</p>
  </a>
</article>

<style>
  .post-preview {
    width: 100%;
    max-width: 42rem;
    margin: 0 auto 6rem;
  }

  .post-link {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  h2 {
    font-size: 2rem;
    font-weight: 700;
    line-height: 1.2;
    margin-bottom: 0.5rem;
    color: var(--text-color);
    text-align: left;
  }

  time {
    display: block;
    color: #666;
    font-size: 1rem;
    margin-bottom: 1.5rem;
    text-align: left;
  }

  .image-container {
    width: 100%;
    margin-bottom: 1.5rem;
    overflow: hidden;
  }

  .image-container img {
    width: 100%;
    height: auto;
    display: block;
  }

  p {
    color: #4a5568;
    font-size: 1.125rem;
    line-height: 1.6;
    text-align: left;
  }

  @media (max-width: 768px) {
    h2 {
      font-size: 1.75rem;
    }

    p {
      font-size: 1rem;
    }
  }
</style>
