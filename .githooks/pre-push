#!/bin/sh

echo "🔄 Pre-push: rebuild production artifact + test..."

# Stop everything (web or preview, whatever is running)
docker compose --profile preview down

# Start only the preview container
docker compose --profile preview up --build -d preview

PRE_PUSH=1 python -m unittest discover tests/

test_exit_code=$?

# Stop preview, start dev
echo "🔄 Switching back to dev mode..."
docker compose --profile preview down
docker compose up -d

if [ $test_exit_code -ne 0 ]; then
    echo "❌ Tests failed. Push blocked."
    exit 1
fi

echo "✅ All checks passed!"
exit 0
