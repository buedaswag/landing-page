#!/bin/sh

echo "Running pre-push checks..."

# Run tests
python -m unittest discover tests/

# Capture the exit code
test_exit_code=$?

# If tests failed, block the push
if [ $test_exit_code -ne 0 ]; then
    echo "❌ Tests failed. Push blocked."
    exit 1
fi

echo "✅ All checks passed!"
exit 0 